services:
  cortex-api:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    ports:
      - "9000:8000"
    volumes:
      - ../models:/app/models:ro
    environment:
      - CLOUD_API_URL=${CLOUD_API_URL:-}
    restart: unless-stopped
